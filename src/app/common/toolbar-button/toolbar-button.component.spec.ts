import { ToolbarButtonComponent } from './toolbar-button.component'
import { shouldProjectContent } from '@/test/helpers/component-testers'

describe('ToolbarButtonComponent', () => {
  shouldProjectContent(ToolbarButtonComponent)
})
