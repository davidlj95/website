<?xml version="1.0" encoding="utf-8"?>
<browserconfig>
  <msapplication>
    <tile>
      {% for size in browserconfigIconSizes %}<square{{ size }}x{{ size }}logo src="/assets/img/favicons/ms-icon-{{ size }}x{{ size }}.png"/>{% unless forloop.last %}
      {% endunless %}{% endfor %}
      <TileColor>{{ themeColor }}</TileColor>
    </tile>
  </msapplication>
</browserconfig>
