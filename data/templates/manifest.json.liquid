{
  "name": "{{ siteName }}",
  "short_name": "{{ nickname }}",
  "description": "{{ description }}",
  "display": "browser",
  "background_color": "{{ themeColor }}",
  "theme_color": "{{ themeColor }}",
  "icons": [
    {%  for iconSize in manifestJsonMaskableIconSizes %}{
      "src": "/images/favicons/maskable-icon-x{{ iconSize }}.png",
      "sizes": "{{ iconSize }}x{{ iconSize }}",
      "type": "image/png",
      "purpose": "any maskable monochrome"
    },
    {%  endfor %}{
      "src": "/images/favicons/maskable-icon.png",
      "sizes": "any",
      "type": "image/png",
      "purpose": "any maskable monochrome"
    }
  ]
}
